server:
  port: 8087

spring:
  application:
    name: ai-service
  cloud:
    nacos:
      discovery:
#        server-addr: localhost:8848
        server-addr: 10.54.0.36:8848
        username: nacos
        password: nacos
  
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/education_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
#    password: password
    password: 20040614aaa@shy
    
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-test-query: SELECT 1
  
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3000

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.edu.platform.ai.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# 本地文件存储配置
local:
  storage:
    # 服务器上文件的物理存储根路径
    path: /home/uadmin/deploy/uploads
    # 对外提供访问的基础URL（学校服务器IP）
    base-url: http://10.54.0.36

# 阿里百炼 AI 配置
ai:
  bailian:
    api-key: ${BAILIAN_API_KEY}
    base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
    analyze:
      model: qwen-long
      max-tokens: 2000
      temperature: 0.3
    recommend:
      model: qwen-plus
      max-tokens: 1000
      temperature: 0.5

# Knife4j 配置
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

knife4j:
  enable: true
  setting:
    language: zh_cn

logging:
  level:
    com.edu.platform.ai: DEBUG

# Feign 超时配置
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 60000
